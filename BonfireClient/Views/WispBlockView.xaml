<UserControl x:Class="BonfireClient.Views.WispBlockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="300">
    <Grid Margin="0,10,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="65"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Margin="0,0,5,0">
            <Image Source="{Binding ImageSource}"></Image>
            <TextBlock x:Name="UserName" HorizontalAlignment="Center" Margin="0,5,0,0"></TextBlock>
        </StackPanel>
        <TextBlock x:Name="User" Grid.Column="0"></TextBlock>
        <ItemsControl x:Name="Wisps" Grid.Column="1" Margin="10,0,0,0"></ItemsControl>
    </Grid>
</UserControl>
